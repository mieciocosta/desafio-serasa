{
  "info": {
    "_postman_id": "e1c8d4c3-1111-4444-8888-0fac7beb60af",
    "name": "SISPI - Recuperacao de Valores - Contrato (GET+POST)",
    "description": "Collection com apenas 2 endpoints (/api/v1/recuperacao-valores GET e POST) focada em testes de contrato.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Recuperacao de Valores (Contrato)",
      "item": [
        {
          "name": "GET /api/v1/recuperacao-valores - contrato",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/recuperacao-valores?cpfCnpj=02914270305",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "recuperacao-valores"
              ],
              "query": [
                {
                  "key": "cpfCnpj",
                  "value": "02914270305"
                }
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// ---------- TESTES DE CONTRATO: GET /api/v1/recuperacao-valores ----------",
                  "const statusEnum = ['CRIADA','RASTREADA','AGUARDANDO_ANALISE','ANALISADA','EM_DEVOLUCAO','CONCLUIDA','CANCELADA','ENVIADA','TENTATIVA_DEVOLUCAO'];",
                  "const tipoFluxoEnum = ['AUTOMATIZADO', 'INTERATIVO'];",
                  "const tipoFraudeEnum = ['GOLPE','INVASAO_CONTA','COACAO','ACESSO_FRAUDULENTO','OUTRO','DESCONHECIDO'];",
                  "",
                  "pm.test('Status code é 200 (contrato)', function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test('Resposta é JSON', function () {",
                  "    pm.response.to.be.withBody;",
                  "    pm.response.to.be.json;",
                  "});",
                  "",
                  "const body = pm.response.json();",
                  "",
                  "pm.test('Body segue contrato: array de ConsultarRecuperacaoValorResponseDto', function () {",
                  "    pm.expect(Array.isArray(body)).to.be.true;",
                  "});",
                  "",
                  "if (Array.isArray(body) && body.length > 0) {",
                  "    const item = body[0];",
                  "    pm.test('Item possui campos mínimos de ConsultarRecuperacaoValorResponseDto', function () {",
                  "        pm.expect(item).to.be.an('object');",
                  "        pm.expect(item).to.have.property('idCorrelacao');",
                  "        pm.expect(item).to.have.property('recuperacaoValor');",
                  "    });",
                  "",
                  "    pm.test('idCorrelacao segue padrão de 32 hex (^[0-9a-fA-F]{32}$)', function () {",
                  "        pm.expect(item.idCorrelacao).to.match(/^[0-9a-fA-F]{32}$/);",
                  "    });",
                  "",
                  "    const rec = item.recuperacaoValor;",
                  "    pm.test('recuperacaoValor possui campos obrigatórios', function () {",
                  "        pm.expect(rec).to.be.an('object');",
                  "        pm.expect(rec).to.have.property('tipoFluxo');",
                  "        pm.expect(rec).to.have.property('tipoFraude');",
                  "        pm.expect(rec).to.have.property('idRecuperacaoValor');",
                  "        pm.expect(rec).to.have.property('situacao');",
                  "    });",
                  "",
                  "    pm.test('tipoFluxo respeita enum de contrato', function () {",
                  "        pm.expect(tipoFluxoEnum).to.include(rec.tipoFluxo);",
                  "    });",
                  "",
                  "    pm.test('tipoFraude respeita enum de contrato', function () {",
                  "        pm.expect(tipoFraudeEnum).to.include(rec.tipoFraude);",
                  "    });",
                  "",
                  "    pm.test('situacao respeita enum de contrato', function () {",
                  "        pm.expect(statusEnum).to.include(rec.situacao);",
                  "    });",
                  "",
                  "    pm.test('isbp, se presente, segue regex ^[0-9]{8}$', function () {",
                  "        if (rec.isbp !== undefined && rec.isbp !== null) {",
                  "            pm.expect(rec.isbp).to.match(/^[0-9]{8}$/);",
                  "        }",
                  "    });",
                  "} else {",
                  "    console.log('Resposta vazia - ainda assim contrato ok (array), mas sem validar itens.');",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/v1/recuperacao-valores - contrato",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"usuario\": \"teste.qa\",\n  \"idTransacaoRaiz\": \"ABCD1234EFGH5678IJKL9012MN\",\n  \"tipoFluxo\": \"AUTOMATIZADO\",\n  \"tipoSituacao\": \"GOLPE\",\n  \"detalhes\": \"Criado via Postman teste contrato.\",\n  \"emailContatoPsp\": \"qa@example.com\",\n  \"telefoneContatoPsp\": \"5598999999999\",\n  \"valorMinimoTransacao\": \"1.00\",\n  \"quantidadeMaximaTransacoes\": \"100\",\n  \"janelaTempoEntreTransacoes\": \"PT24H\",\n  \"profundidadeGrafo\": \"3\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/recuperacao-valores",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "v1",
                "recuperacao-valores"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// ---------- TESTES DE CONTRATO: POST /api/v1/recuperacao-valores ----------",
                  "const statusEnum = ['CRIADA','RASTREADA','AGUARDANDO_ANALISE','ANALISADA','EM_DEVOLUCAO','CONCLUIDA','CANCELADA','ENVIADA','TENTATIVA_DEVOLUCAO'];",
                  "const tipoFluxoEnum = ['AUTOMATIZADO', 'INTERATIVO'];",
                  "const tipoSituacaoEnum = ['GOLPE','INVASAO_CONTA','COACAO','ACESSO_FRAUDULENTO','OUTRO','DESCONHECIDO'];",
                  "",
                  "pm.test('Status code é 200 (contrato)', function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test('Resposta é JSON', function () {",
                  "    pm.response.to.be.withBody;",
                  "    pm.response.to.be.json;",
                  "});",
                  "",
                  "const body = pm.response.json();",
                  "",
                  "pm.test('Body segue contrato mínimo de RecuperacaoValoresRespostaDto', function () {",
                  "    pm.expect(body).to.be.an('object');",
                  "    pm.expect(body).to.have.property('idCorrelacao');",
                  "    pm.expect(body).to.have.property('horaResposta');",
                  "    pm.expect(body).to.have.property('recuperacaoValores');",
                  "});",
                  "",
                  "pm.test('recuperacaoValores possui estrutura conforme contrato', function () {",
                  "    const recVals = body.recuperacaoValores;",
                  "    pm.expect(recVals).to.be.an('object');",
                  "    pm.expect(recVals).to.have.property('id');",
                  "    pm.expect(recVals).to.have.property('status');",
                  "    pm.expect(recVals).to.have.property('tipoFluxo');",
                  "    pm.expect(recVals).to.have.property('idTransacaoRaiz');",
                  "    pm.expect(recVals).to.have.property('tipoSituacao');",
                  "    pm.expect(recVals).to.have.property('detalhesRelato');",
                  "    pm.expect(recVals).to.have.property('participanteReportante');",
                  "    pm.expect(recVals).to.have.property('dataHoraCriacao');",
                  "    pm.expect(recVals).to.have.property('ultimaModificacao');",
                  "});",
                  "",
                  "pm.test('status/tipoFluxo/tipoSituacao respeitam enums de contrato', function () {",
                  "    const recVals = body.recuperacaoValores;",
                  "    pm.expect(statusEnum).to.include(recVals.status);",
                  "    pm.expect(tipoFluxoEnum).to.include(recVals.tipoFluxo);",
                  "    pm.expect(tipoSituacaoEnum).to.include(recVals.tipoSituacao);",
                  "});",
                  "",
                  "pm.test('informacoesContato, se presente, é objeto', function () {",
                  "    const recVals = body.recuperacaoValores;",
                  "    if (recVals.informacoesContato !== undefined && recVals.informacoesContato !== null) {",
                  "        pm.expect(recVals.informacoesContato).to.be.an('object');",
                  "    }",
                  "});",
                  "",
                  "// Guarda id/idCorrelacao pra outros testes (se você quiser criar depois)",
                  "pm.collectionVariables.set('idCorrelacao', body.idCorrelacao);",
                  "if (body.recuperacaoValores && body.recuperacaoValores.id) {",
                  "    pm.collectionVariables.set('idRecuperacaoValor', body.recuperacaoValores.id);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    }
  ],
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8080",
      "type": "string"
    },
    {
      "key": "authToken",
      "value": "<INSIRA_SEU_JWT_AQUI>",
      "type": "string"
    }
  ]
}
